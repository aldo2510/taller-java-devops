// comentario
pipeline {
    // Cualquier agente
    agent none

    stages {
        stage('Build') {
            agent any
            steps {
                sh 'mvn package'
            }
        }
    }
  
  
}
